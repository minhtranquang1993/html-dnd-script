{
    "tokens": {
        "classic": {
            "env_var": "GITHUB_TOKEN_CLASSIC",
            "file_path": "credentials/github_token.txt",
            "_comment": "Personal access token (classic) — priority 1. Dùng env_var HOẶC file_path, ưu tiên file_path nếu có"
        },
        "finegrained": {
            "env_var": "GITHUB_TOKEN_FINEGRAINED",
            "file_path": "credentials/github_token_fire_gains.txt",
            "_comment": "Fine-grained personal access token — fallback"
        }
    },
    "repo_overrides": {
        "_comment": "Map specific owner/repo to preferred token alias. Example below.",
        "minhtranquang1993/private-repo": "classic"
    }
}